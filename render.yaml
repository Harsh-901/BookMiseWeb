services:
  - type: web
    name: bookmise-backend
    runtime: python
    plan: free
    region: oregon

    rootDir: Backend            # ← match the real folder case

    buildCommand: pip install -r requirements.txt
    startCommand: >
      gunicorn --bind 0.0.0.0:$PORT main:app   # works with your current main.py

    envVars:
      - key: PYTHON_VERSION
        value: "3.10"

      - key: SUPABASE_URL
        value: https://ywrtpfmuctnflfbhplmb.supabase.co

      - key: SUPABASE_SERVICE_KEY
        sync: false           # set the secret in the dashboard only

      - key: PYTHONUNBUFFERED
        value: "1"
